import { SimplebarDirective } from './simplebar.directive';

describe('SimplebarDirective', () => {
  it('should create an instance', () => {
    const directive = new SimplebarDirective();
    expect(directive).toBeTruthy();
  });
});
