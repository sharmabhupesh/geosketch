import { TomselectDirective } from './tomselect.directive';

describe('TomselectDirective', () => {
  it('should create an instance', () => {
    const directive = new TomselectDirective();
    expect(directive).toBeTruthy();
  });
});
